# --- Azure OpenAI ---
AZURE_OPENAI_API_KEY=your-azure-openai-key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_VERSION=2024-06-01
AZURE_OPENAI_DEPLOYMENT_NAME=your-deployment-name

# --- Application ---
DEBUG=true

# --- Security ---
# API key for authenticating frontend requests. Leave empty in dev to skip auth.
API_KEY=

# --- JWT ---
# MUST change in production. Used to sign access and refresh tokens.
JWT_SECRET=change-me-in-production
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# --- CORS ---
# Allowed origins. Accepts JSON array or comma-separated values.
# Examples:
#   CORS_ORIGINS=http://localhost:5173
#   CORS_ORIGINS=https://my-frontend.netlify.app,https://other-origin.com
#   CORS_ORIGINS=["https://my-frontend.netlify.app"]

# --- Rate Limiting ---
RATE_LIMIT_DEFAULT=60/minute
RATE_LIMIT_GENERATE=10/minute
